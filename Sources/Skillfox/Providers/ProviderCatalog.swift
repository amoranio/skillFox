enum ProviderCatalog {
    static let all: [ProviderDefinition] = [
        ClaudeProvider.definition,
        CopilotProvider.definition,
        CodexProvider.definition,
        GeminiProvider.definition,
        AntigravityProvider.definition,
        OpenCodeProvider.definition,
    ]
}
